/** @type {import('next').NextConfig} */
const nextConfig = {
  // The i18n config has been removed as we're now using the App Router approach
  // for internationalization with client-side locale switching
}

module.exports = nextConfig
